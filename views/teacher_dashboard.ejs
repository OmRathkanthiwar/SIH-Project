<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Dashboard - Eduventure</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-layout">
        <%- include('partials/sidebar') %>
        <main class="main-content">
            <h1 class="text-3xl font-bold"><%= greeting %>, <%= user.details.name %>!</h1>
            <p class="text-foreground/70 mb-8">Welcome to your command center.</p>

            <!-- Stats Cards -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                <div class="card p-6 flex items-center justify-between">
                    <div>
                        <p class="text-gray-400">Total Students</p>
                        <p class="text-3xl font-bold text-blue-300"><%= students.length %></p>
                    </div>
                    <div class="text-4xl">🧑‍🎓</div>
                </div>
                <div class="card p-6 flex items-center justify-between">
                    <div>
                        <p class="text-gray-400">Total Quizzes</p>
                        <p class="text-3xl font-bold text-blue-300"><%= quizzes.length %></p>
                    </div>
                    <div class="text-4xl">📝</div>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="card p-6">
                <h2 class="text-2xl font-semibold mb-4 text-blue-300">Quick Actions</h2>
                <div class="flex gap-4">
                    <a href="/teacher/students" class="btn-primary flex-1 text-center py-3">Manage Students</a>
                    <a href="/teacher/quizzes" class="btn-primary flex-1 text-center py-3">Manage Quizzes</a>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
