<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= t.aiTutor %> - <%= t.eduventure %></title>
<script src="https://cdn.tailwindcss.com"></script>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-layout">
        <%- include('partials/sidebar') %>
        <main class="main-content">
            <h1 class="text-3xl font-bold mb-6 text-blue-400">🤖 <%= t.askAITutor %></h1>
            <div class="card p-6">
                <form action="/ask-ai" method="POST">
                    <label for="question" class="block text-sm font-medium text-gray-300 mb-2"><%= t.askAQuestion %></label>
                    <div class="flex items-center gap-4">
                        <input type="text" id="question" name="question" placeholder="<%= t.askPlaceholder %>" required value="<%= question || '' %>" class="input-field flex-grow">
                        <button type="submit" class="btn-primary"><%= t.ask %></button>
                    </div>
                </form>

                <% if (question && answer) { %>
                    <div class="mt-8 p-6 rounded-lg bg-gray-800/50 border border-gray-700">
                        <p class="font-semibold text-lg text-gray-300"><%= t.yourQuestion %>:</p>
                        <p class="mb-4 text-gray-400"><%= question %></p>
                        <hr class="border-gray-600 my-4">
                        <p class="font-semibold text-lg text-blue-400"><%= t.aiAnswer %>:</p>
                        <p class="text-gray-300 whitespace-pre-line"><%= answer %></p>
                    </div>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>

