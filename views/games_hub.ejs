<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.gamesHub %> - <%= t.eduventure %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-layout">
        <%- include('partials/sidebar') %>
        <main class="main-content">
            <header class="mb-8 text-center">
                <h1 class="text-4xl font-bold text-blue-400">🎮 <%= t.gamesHub %> 🎮</h1>
                <p class="text-foreground/70 mt-2"><%= t.chooseAGame %></p>
            </header>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <!-- Memory Game Card -->
                <div class="card text-center p-6 transform hover:-translate-y-2 transition-transform duration-300">
                    <div class="text-6xl mb-4">🧠</div>
                    <h2 class="text-2xl font-bold mb-2"><%= t.memoryGame %></h2>
                    <p class="text-gray-400 mb-4"><%= t.memoryGameDesc %></p>
                    <a href="/games/memory" class="btn-primary inline-block"><%= t.playNow %></a>
                </div>

                <!-- Math Sprint Card -->
                <div class="card text-center p-6 transform hover:-translate-y-2 transition-transform duration-300">
                    <div class="text-6xl mb-4">➕</div>
                    <h2 class="text-2xl font-bold mb-2"><%= t.mathSprint %></h2>
                    <p class="text-gray-400 mb-4"><%= t.mathSprintDesc %></p>
                    <a href="/games/math-sprint" class="btn-primary inline-block"><%= t.playNow %></a>
                </div>

                <!-- Typing Speed Test Card -->
                <div class="card text-center p-6 transform hover:-translate-y-2 transition-transform duration-300">
                    <div class="text-6xl mb-4">⌨️</div>
                    <h2 class="text-2xl font-bold mb-2"><%= t.typingSpeedTest %></h2>
                    <p class="text-gray-400 mb-4"><%= t.typingSpeedTestDesc %></p>
                    <a href="/games/typing-speed" class="btn-primary inline-block"><%= t.playNow %></a>
                </div>
            </div>
        </main>
    </div>
</body>
</html>

