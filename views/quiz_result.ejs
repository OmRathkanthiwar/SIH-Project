<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= t.quizResults %></title>
  <link rel="stylesheet" href="/css/quiz_result.css">
</head>
<body>
  <div class="quiz-container">
    <h1 class="result-title">🎉 <%= t.quizResults %> 🎉</h1>

    <div class="result-score-card">
      <h2><%= t.yourScore %></h2>
      <div class="score-circle">
        <span><%= score %> / <%= total %></span>
      </div>
    </div>

    <div class="result-summary">
      <h2><%= t.answerReview %></h2>
      <ul>
        <% results.forEach((q, index) => { %>
          <li class="<%= q.correct ? 'correct' : 'incorrect' %>">
            <strong>Q<%= index+1 %>:</strong> <%= q.question %> <br>
            <span><%= t.yourAnswer %>:</span> <%= q.userAnswer %> <br>
            <span><%= t.correctAnswer %>:</span> <%= q.correctAnswer %>
          </li>
        <% }) %>
      </ul>
    </div>

    <div class="result-actions">
      <a href="/quiz/<%= quizId %>" class="btn retry">🔄 <%= t.tryAgain %></a>
      <a href="/student" class="btn dashboard">🏠 <%= t.dashboard %></a>
    </div>
  </div>
</body>
</html>

