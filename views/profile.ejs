<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.myProfile %> - <%= t.eduventure %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-layout">
        <%- include('partials/sidebar') %>
        <main class="main-content">
            <h1 class="text-3xl font-bold mb-6 text-blue-400"><%= t.myProfile %></h1>
            <div class="card p-6 space-y-3 text-lg">
                <p><strong><%= t.name %>:</strong> <%= user.details.name %></p>
                <p><strong><%= t.grade %>:</strong> <%= user.details.grade %></p>
                <p><strong><%= t.adventurePoints %>:</strong> <%= user.details.adventurePoints %></p>
                
                <p><strong><%= t.badges %>:</strong>
                    <% if (user.details.badges && user.details.badges.length > 0) { %>
                        <%= user.details.badges.join(', ') %>
                    <% } else { %>
                        <span class="text-gray-500"><%= t.noBadges %></span>
                    <% } %>
                </p>

            </div>
        </main>
    </div>
</body>
</html>

